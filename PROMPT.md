# Starting Prompt (give IDENTICAL text + screenshot to both models)

Repo: https://github.com/Chiheb-AouledAhmed/ui-bug-tablet-nav.git
Commit: PLACEHOLDER

Clicking a nav link like "Team" scrolls the page down, but the section
heading ends up hidden underneath the fixed header instead of appearing
just below it, as shown in the attached screenshot (taken right after
clicking "Team"). The page's initial layout is fine — the header doesn't
overlap anything when you first open the page, this only happens after
clicking a nav link.

Don't change what happens on initial page load.
